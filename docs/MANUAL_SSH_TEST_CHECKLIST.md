# SSH集成测试 - 手动测试清单

## 🎯 测试目标

在真实SSH环境（example.com）上验证终端模拟器的功能。

---

## 📋 前置准备

### 1. 启动应用
```bash
.\gradlew.bat :composeApp:run
```

### 2. 连接到服务器
- 服务器: example.com
- 用户: root
- 端口: 22

---

## ✅ 测试清单

### 测试1: 基础命令
**目的**: 验证基本SSH连接和命令执行

1. 输入: `echo "Hello Terminal"`
   - ✅ 输出显示 "Hello Terminal"
   
2. 输入: `pwd`
   - ✅ 显示当前目录

3. 输入: `ls --color=always`
   - ✅ 文件列表显示
   - ✅ 有颜色高亮

4. 输入: `whoami`
   - ✅ 显示 "root"

---

### 测试2: Nano编辑器  ⭐ 重点
**目的**: 验证alternate screen buffer和编辑功能

#### 2.1 启动和界面
1. 输入: `nano /tmp/nano_test.txt`
2. ✅ 屏幕切换到nano界面
3. ✅ 顶部显示标题栏
4. ✅ 底部显示帮助菜单

#### 2.2 编辑功能
5. 输入一些文本: `This is line 1`
6. ✅ 文本正常显示
7. 按 `Enter` 插入新行
8. ✅ 光标移到新行
9. ✅ 上一行文本保持显示
10. 输入: `This is line 2`
11. ✅ 第二行文本正常显示

#### 2.3 多行编辑
12. 继续按 `Enter` 并输入几行文本
13. ✅ 每一行都正常显示
14. ✅ 光标位置正确
15. 使用方向键上下移动
16. ✅ 光标移动正确

#### 2.4 保存和退出
17. 按 `Ctrl+X` 退出
18. ✅ 底部提示保存确认
19. 按 `y` 确认保存
20. ✅ 提示输入文件名
21. 按 `Enter` 确认
22. ✅ 返回到主屏幕
23. ✅ 显示之前的命令提示符

#### 2.5 验证保存
24. 输入: `cat /tmp/nano_test.txt`
25. ✅ 显示刚才编辑的内容
26. ✅ 内容完整无误

**测试结果**: [ ] 通过 / [ ] 失败

---

### 测试3: Vim编辑器
**目的**: 验证vim的模式切换和编辑

#### 3.1 启动vim
1. 输入: `vim /tmp/vim_test.txt`
2. ✅ 进入vim界面
3. ✅ 显示波浪线 (~)

#### 3.2 插入模式
4. 按 `i` 进入插入模式
5. ✅ 底部显示 "-- INSERT --" （如果vim配置显示）
6. 输入: `Testing vim editor`
7. ✅ 文本正常显示

#### 3.3 保存退出
8. 按 `ESC` 返回命令模式
9. 输入: `:wq` 并按 `Enter`
10. ✅ 返回主屏幕
11. 输入: `cat /tmp/vim_test.txt`
12. ✅ 显示刚才的文本

**测试结果**: [ ] 通过 / [ ] 失败

---

### 测试4: Less分页器
**目的**: 验证分页显示功能

1. 创建测试文件: `seq 1 100 > /tmp/numbers.txt`
2. 输入: `less /tmp/numbers.txt`
3. ✅ 显示文件内容（分页）
4. ✅ 底部显示冒号 (`:`)
5. 按 `Space` 或 `Page Down`
6. ✅ 向下翻页
7. 按 `q` 退出
8. ✅ 返回主屏幕

**测试结果**: [ ] 通过 / [ ] 失败

---

### 测试5: Yazi文件管理器 ⭐ 新增重点
**目的**: 验证现代TUI文件管理器

#### 5.1 检查安装
1. 输入: `which yazi`
2. 如果显示路径，说明已安装
3. 如果显示 "not found"，跳过此测试

#### 5.2 启动yazi
4. 输入: `yazi`
5. ✅ 进入yazi界面
6. ✅ 显示文件列表
7. ✅ 界面布局正确

#### 5.3 导航功能
8. 按 `j` (向下)
9. ✅ 选择光标向下移动
10. 按 `k` (向上)
11. ✅ 选择光标向上移动
12. 按 `l` 或 `Enter` (进入目录)
13. ✅ 如果是目录，进入该目录
14. 按 `h` (返回上级)
15. ✅ 返回上级目录

#### 5.4 退出
16. 按 `q` 退出
17. ✅ 返回主屏幕
18. ✅ 显示命令提示符

**测试结果**: [ ] 通过 / [ ] 失败 / [ ] 跳过（未安装）

---

### 测试5.5: Yazi图片预览 ⭐ 重点测试
**目的**: 验证Yazi的图片预览功能

#### 5.5.1 检查图片文件
1. 确认服务器上有图片文件:
   ```bash
   ls -lh /home/testuser/pic.jpg
   ```
2. ✅ 显示文件存在（约109字节）

#### 5.5.2 启动Yazi并定位图片
3. 输入: `cd /root`
4. 输入: `yazi`
5. ✅ 进入yazi界面
6. 使用 `j`/`k` 移动到 `pic.jpg`
7. ✅ 看到 `pic.jpg` 被高亮选中

#### 5.5.3 查看图片预览
8. 在选中 `pic.jpg` 时，查看右侧预览区
9. ✅ **关键验证**: 右侧应该显示图片预览
   - 如果支持图形协议（Sixel/Kitty），会显示完整图片
   - 如果不支持，可能显示：
     - Unicode块字符组成的缩略图
     - 或图片信息（尺寸、类型等）
     - 或文本"[Image]"

#### 5.5.4 验证显示内容
10. ✅ 检查是否有任何图片相关的显示
    - [ ] 能看到图形/块字符
    - [ ] 能看到图片信息（大小、格式）
    - [ ] 至少显示图片占位符

11. 截图记录显示效果

#### 5.5.5 测试多种图片格式（可选）
12. 如果有其他图片，测试PNG/GIF等格式:
    ```bash
    # 创建测试文件（如果需要）
    cp pic.jpg test.png
    ```
13. ✅ 验证不同格式的预览效果

#### 5.5.6 退出
14. 按 `q` 退出yazi
15. ✅ 返回主屏幕

**测试结果**: [ ] 通过 / [ ] 失败 / [ ] 跳过（未安装）

**预览效果**: 
- [ ] 完整图片显示
- [ ] 块字符预览
- [ ] 仅文本信息
- [ ] 无预览

**备注**: ___________

---

### 测试6: Git命令
**目的**: 验证彩色输出

1. 创建测试仓库:
   ```bash
   mkdir /tmp/git_test && cd /tmp/git_test
   git init
   echo "test" > file.txt
   git add file.txt
   git commit -m "initial"
   ```

2. 修改文件: `echo "modified" > file.txt`

3. 查看diff: `git diff --color=always file.txt`
4. ✅ 显示diff输出
5. ✅ 有颜色显示（红色删除，绿色添加）

6. 清理: `cd ~ && rm -rf /tmp/git_test`

**测试结果**: [ ] 通过 / [ ] 失败

---

### 测试7: Htop系统监控（可选）
**目的**: 验证实时监控界面

1. 输入: `which htop`
2. 如果未安装，跳过此测试

3. 输入: `htop`
4. ✅ 显示系统监控界面
5. ✅ 显示CPU/内存使用率
6. ✅ 显示进程列表
7. ✅ 界面持续更新
8. 按 `q` 退出
9. ✅ 返回主屏幕

**测试结果**: [ ] 通过 / [ ] 失败 / [ ] 跳过（未安装）

---

### 测试8: 长时间运行和Ctrl+C
**目的**: 验证信号处理

1. 输入: `ping 8.8.8.8`
2. ✅ 显示ping输出
3. ✅ 持续更新
4. 按 `Ctrl+C` 中断
5. ✅ ping停止
6. ✅ 返回命令提示符

**测试结果**: [ ] 通过 / [ ] 失败

---

### 测试9: 中文显示（可选）
**目的**: 验证中文字符显示

1. 输入: `echo "你好世界"`
2. ✅ 中文正确显示
3. ✅ 字符对齐正确

4. 输入: `echo "Test测试Mixed混合"`
5. ✅ 中英文混合正确显示

**测试结果**: [ ] 通过 / [ ] 失败

---

### 测试10: 窗口大小调整（可选）
**目的**: 验证终端大小调整

1. 调整应用窗口大小
2. 输入: `tput cols` (显示列数)
3. ✅ 显示当前列数
4. 输入: `tput lines` (显示行数)
5. ✅ 显示当前行数
6. 再次调整窗口
7. 重复步骤2-5
8. ✅ 数值更新正确

**测试结果**: [ ] 通过 / [ ] 失败

---

## 📊 测试总结

### 测试统计
- 总测试数: 10
- 通过: ___
- 失败: ___
- 跳过: ___

### 关键功能验证

#### ✅ 必须通过
- [ ] 基础命令执行
- [ ] Nano编辑器（含编辑和保存）
- [ ] Alternate screen buffer切换
- [ ] 颜色显示

#### ⭐ 重点验证
- [ ] Nano多行编辑
- [ ] Yazi文件管理器
- [ ] 光标位置恢复

#### ⬜ 可选验证
- [ ] Htop实时监控
- [ ] Vim编辑器
- [ ] 窗口大小调整

---

## 🐛 问题记录

如果测试失败，请记录：

### 问题1
- **测试项**: ___________
- **现象**: ___________
- **预期**: ___________
- **实际**: ___________
- **截图**: ___________

### 问题2
- **测试项**: ___________
- **现象**: ___________
- **预期**: ___________
- **实际**: ___________

---

## 📝 测试备注

### 环境信息
- 测试日期: ___________
- 应用版本: ___________
- 服务器: example.com
- 网络延迟: ___________

### 额外观察
- ___________
- ___________
- ___________

---

## 🎯 测试完成标准

### 基础标准（必须）
- ✅ 所有P0测试通过
- ✅ Nano编辑功能正常
- ✅ Alternate screen正常切换
- ✅ 无严重显示问题

### 优秀标准
- ✅ 所有测试通过
- ✅ Yazi正常工作
- ✅ 所有颜色正确显示
- ✅ 无任何显示问题

---

## 📞 后续行动

### 如果全部通过 ✅
1. 记录测试结果
2. 更新测试文档
3. 标记为生产就绪

### 如果有失败 ❌
1. 记录详细错误信息
2. 收集日志和截图
3. 创建bug报告
4. 优先修复关键问题

---

**测试人**: ___________  
**测试日期**: ___________  
**签名**: ___________

---

**快速开始测试**: 
```bash
# 1. 启动应用
.\gradlew.bat :composeApp:run

# 2. 连接到服务器
# 使用UI连接到 testuser@example.com:22

# 3. 按照清单逐项测试
```

**祝测试顺利！** 🚀

